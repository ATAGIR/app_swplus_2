import 'package:flutter/material.dart';

class PageProducto extends StatelessWidget {
  const PageProducto({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Productos'),
      ),
    );
  }
}

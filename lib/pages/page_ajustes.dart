import 'package:flutter/material.dart';

class PageAjustes extends StatelessWidget {
  const PageAjustes({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Ajustes'),
      ),
    );
  }
}
